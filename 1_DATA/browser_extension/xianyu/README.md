# AI_FACTORY_OS Xianyu Collector Extension v1 (Entry 065)

Load unpacked in Chrome:

1. Open `chrome://extensions`
2. Enable **Developer mode**
3. **Load unpacked** → select `1_DATA/browser_extension/xianyu/`

Before collecting, start the Local Bridge:

```bash
python 1_DATA/connectors/xianyu_extension_bridge_065.py
```

Then open a normal Xianyu search results page in Chrome, click the extension, and **Start**.

Output (TEST_MODE): `1_DATA/_tests/xianyu_extension_065/`
